# Nvidia-Stock-LSTM
Built an LSTM stock price prediction model in PyTorch using historical market data. Features include data normalization, validation-based model checkpointing, performance evaluation (R², MAE, RMSE), visualization of predictions vs. actual prices, and future stock price forecasting using recursive predictions.
